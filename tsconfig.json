{
  "files": [
    "src/index.ts"
  ],
  "compilerOptions": {
    "noImplicitAny": true,
    "sourceMap": true,
    "target": "es6",
    "module": "commonjs",
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "stupid-player": ["src/plugins/media/stupidPlayer.d.ts"] // This mapping is relative to "baseUrl"
    }
  },
  "types": [
    "node",
    "express",
    "uuid"
  ],
  "typeroots": [
    "../node_modules/@types",
    "../node_modules/@reactivex"
  ]
}
